/* Type-check fallback; Metro resolves storage.native.ts (document-directory file, SQLCipher-derived key) or storage.web.ts. */
export { cacheStorage, randomNonce, storageSecretHex, writeLicenceResult } from "./storage.web";

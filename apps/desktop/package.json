{
  "name": "@inborn/desktop",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "bash scripts/with-signing-identity.sh tauri build",
    "build:app": "bash scripts/with-signing-identity.sh tauri build --bundles app"
  },
  "devDependencies": {
    "@tauri-apps/cli": "2.11.4"
  }
}

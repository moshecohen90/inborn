[package]
name = "inborn-desktop"
version = "0.0.1"
description = "Inborn desktop shell: a Tauri v2 window around the Expo web build"
authors = ["Inborn"]
edition = "2021"
rust-version = "1.77.2"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }

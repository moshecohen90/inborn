{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "The main window: app commands (engine, store, vault) and events only. No fs/http/shell permissions — the webview never touches the disk or the network directly.",
  "windows": [
    "main"
  ],
  "permissions": [
    "core:default"
  ]
}

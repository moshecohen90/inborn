/** Types for the generator, so the guard in test/locales.test.ts can import it without a cast. */
export declare const pseudo: (s: string) => string;
export declare function build(): Record<string, string>;

# inbornapp.com and app.inbornapp.com are live — 24.9.2026

Both origins serve. The block recorded in `curl-evidence.md` was one missing API-token permission, and behind it one
bug in the deploy script: the `_headers` file was uploaded as an ordinary asset, so the edge served it and applied
nothing. Both are fixed; every line below is a real response from the public internet.

Account `9de3aac0325f2ec6294e00714a0772b7`, zone `inbornapp.com` = `ef6602f264e66593cd9d21a18809258a`.
Deployed with `node scripts/deploy-cloudflare.mjs --site --app`.

## 1. The token

The Keychain token was read-only on Workers. A new custom token replaces it:

| Permission | Level | Resource |
|---|---|---|
| Account → Workers Scripts | Edit | `9de3aac0325f2ec6294e00714a0772b7` |
| Zone → Workers Routes | Edit | `inbornapp.com` |
| Zone → DNS | Edit | `inbornapp.com` |
| Zone → Zone | Read | `inbornapp.com` |

Name `inborn-deploy-2026-09-24`, id `b1b6b63fa8a6e7de4ae7176c7ea8b065`, no expiry, no IP filter. Stored in the
macOS Keychain under service `inborn-cloudflare-api` (accounts `token` and `inborn`, so the script's
`security find-generic-password -s inborn-cloudflare-api -w` returns it); the value was never printed. The previous
read-only token is kept under service `inborn-cloudflare-api-old-readonly` rather than overwritten.

First proof it is not read-only, the exact call that used to answer `403`:

```
POST /accounts/{acc}/workers/scripts/inborn-probe/assets-upload-session   ->  200  (upload-session JWT returned)
```

**Cloudflare's dashboard cannot create this token.** In the current dashboard, Zone Resources → **"Specific zone" is
rendered disabled** (`aria-disabled="true"`) on the Create Custom Token form, whatever the account resource or the
permission rows are, so a zone-scoped token cannot be built by clicking. The token was created by the dashboard's own
`POST /api/v4/user/tokens` call with the zone resource set to `com.cloudflare.api.account.zone.ef66…258a`; the same
call made from outside the page is refused by the WAF with a `403` challenge page, so it has to originate in the
dashboard. If this token ever has to be rebuilt, expect that, and check the option's state before assuming a
mis-click.

## 2. The deploy-script bug: `_headers` was an asset, not configuration

`readDist` put `/_headers` in the uploaded manifest. Workers static assets then served it verbatim at
`/_headers` (`200`) and applied none of it: the app origin answered **with no `Cross-Origin-Opener-Policy`, no
`Cross-Origin-Embedder-Policy` and no `Content-Security-Policy`** — which is exactly the isolation pair the WASM
engine needs for `SharedArrayBuffer`, so the first deploy would have shipped an origin quietly stuck on the
single-thread fallback. The file now travels as `assets.config._headers` (what wrangler sends) and is kept out of the
manifest.

`/_redirects` is dropped rather than forwarded: the web build writes the Pages-style `/* /index.html 200`, and
Cloudflare rejects it here — `400 100324 Invalid _redirects configuration: Line 1: Infinite loop detected in this
rule` — because `not_found_handling: single-page-application` already is that rule.

Before and after on `https://app.inbornapp.com/`:

| | before | after |
|---|---|---|
| `Cross-Origin-Opener-Policy` | absent | `same-origin` |
| `Cross-Origin-Embedder-Policy` | absent | `require-corp` |
| `Content-Security-Policy` | absent | the full policy from `apps/web/headers.mjs` |
| `GET /_headers` | `200`, the file itself | `200` SPA shell (site: `404`) |

## 3. Live: inbornapp.com (Worker `inborn-site`, `404-page`)

| path | status | content-type | bytes |
|---|---|---|---|
| `/` | 200 | text/html | 29210 |
| `/privacy` | 200 | text/html | 19208 |
| `/terms` | 200 | text/html | 17617 |
| `/accessibility` | 200 | text/html | 16508 |
| `/licenses` | 200 | text/html | 23695 |
| `/proof` | 200 | text/html | 10986 |
| `/support` | 200 | text/html | 9521 |
| `/blog` | 200 | text/html | 7828 |
| `/blog/why-on-device` | 200 | text/html | 11053 |
| `/blog/how-the-proof-works` | 200 | text/html | 11210 |
| `/blog/choosing-a-model` | 200 | text/html | 12159 |
| `/sitemap.xml` | 200 | application/xml | 1058 |
| `/llms.txt` | 200 | text/plain | 4742 |
| `/robots.txt` | 200 | text/plain | 1679 |
| `/no-such-page` | 404 | | the 404 page |
| `/_headers` | 404 | | not served |

Response headers on `/`:

```
content-security-policy: default-src 'none'; script-src 'none'; style-src 'self'; font-src 'self'; img-src 'self' data:; base-uri 'none'; form-action https://app.inbornapp.com; frame-ancestors 'none'; upgrade-insecure-requests
cross-origin-opener-policy: same-origin
cross-origin-resource-policy: same-origin
permissions-policy: camera=(), microphone=(), geolocation=(), interest-cohort=()
referrer-policy: no-referrer
x-content-type-options: nosniff
x-frame-options: DENY
```

## 4. Live: www.inbornapp.com (Worker `inborn-www-redirect`)

| request | status | Location |
|---|---|---|
| `https://www.inbornapp.com/` | 301 | `https://inbornapp.com/` |
| `https://www.inbornapp.com/blog` | 301 | `https://inbornapp.com/blog` |
| `https://www.inbornapp.com/privacy?x=1` | 301 | `https://inbornapp.com/privacy?x=1` |

Path and query survive, which is what the `_redirects` file could not do.

## 5. Live: app.inbornapp.com (Worker `inborn-app`, `single-page-application`)

| path | status | content-type | bytes |
|---|---|---|---|
| `/` | 200 | text/html | 2580 |
| `/?q=hello` | 200 | text/html | 2580 |
| `/paywall` | 200 | text/html | 2580 |
| `_expo/static/js/web/index-cae8470374ddadf4b344f4aa42f30431.js` | 200 | text/javascript | 4013244 |
| `/wllama/wllama.wasm` | 200 | application/wasm | 8457512 |
| `/sw.js` | 200 | text/javascript | |
| `/manifest.webmanifest` | 200 | application/manifest+json | |
| `/hashes.json` | 200 | application/json | |

The shell is the same 2580 bytes on every route because the hand-off is client-side; the bundle and the WASM engine
are the bytes that prove the origin is real, not a placeholder.

Response headers on `/`:

```
content-security-policy: default-src 'self'; script-src 'self' 'wasm-unsafe-eval' blob:; worker-src 'self' blob:; style-src 'self' 'unsafe-inline'; img-src 'self' data: blob:; font-src 'self' data:; connect-src 'self' https://models.inbornapp.com; manifest-src 'self'; object-src 'none'; base-uri 'self'; frame-ancestors 'none'; form-action 'none'
cross-origin-embedder-policy: require-corp
cross-origin-opener-policy: same-origin
cross-origin-resource-policy: same-origin
permissions-policy: camera=(), microphone=(), geolocation=(), payment=(), usb=(), interest-cohort=()
referrer-policy: no-referrer
x-content-type-options: nosniff
x-frame-options: DENY
```

Per-path cache rules from `_headers` arrive too, which is the second proof the file is being parsed and not served:

| path | cache-control |
|---|---|
| `_expo/static/js/web/index-…js` | `public, max-age=31536000, immutable` |
| `/sw.js`, `/hashes.json`, `/manifest.webmanifest` | `no-cache` |
| `/` | `public, max-age=0, must-revalidate` |

## 6. DNS

Attaching each hostname to its Worker wrote the proxied record; nothing was written by hand.

| name | A records | Worker |
|---|---|---|
| `inbornapp.com` | `104.21.70.137`, `172.67.223.209` | `inborn-site` |
| `www.inbornapp.com` | same | `inborn-www-redirect` |
| `app.inbornapp.com` | same | `inborn-app` |
| `models.inbornapp.com` | same | R2 catalogue (untouched) |

`GET /accounts/{acc}/workers/domains` lists exactly those three, all `production`.

**One caching artefact, not a fault.** `app.inbornapp.com` was `NXDOMAIN` for months, so resolvers asked before the
deploy hold a negative answer until the zone's 1800 s SOA minimum expires. Cloudflare's resolver and the zone's own
nameservers returned the new records immediately; Google Public DNS, which is this Mac's first resolver, answered
`NXDOMAIN` for about half an hour and then returned `188.114.97.7` / `188.114.96.7`. Both of those edge addresses
serve `200` on the apex and on the app, so the hostname is public. The checks above were made with
`curl --resolve` while the local resolver was still holding the stale answer; nothing about either origin depends on
it, and macOS clears its own copy without help.

---

# Second pass, same day: HSTS, /download, and two review findings that were already fixed

Two reviews landed after the deploy above. Both were re-checked live before anything was changed.

## What the reviews reported, and what the origins actually sent

| Reported | Live at the time of re-check |
|---|---|
| "inbornapp.com sends NO security headers" | CSP, COOP, CORP, Permissions-Policy, Referrer-Policy, X-Content-Type-Options, X-Frame-Options all present |
| "`/_headers` is downloadable at 200 with the CSP inside" | `404` on the site |
| "app.inbornapp.com has NO DNS record, the app half never went live" | `A 104.21.70.137 / 172.67.223.209` from the zone's own nameservers and from Cloudflare's resolver, `200` with COOP + COEP on all four published edge addresses |

Both reviews were reading the state left by the **first** deploy of the evening, which did ship `_headers` as an
asset; the fix in the section above had already landed by the time they were written. The DNS finding is the
negative-cache artefact documented above: the reviewer's resolver, like this Mac's, still held the pre-deploy
`NXDOMAIN`. Nothing was rebuilt on the strength of either report.

## What was genuinely missing, and is now done

**1. HSTS.** Neither `_headers` file asked for `Strict-Transport-Security`, so neither origin sent it. Added to
`apps/site/public/_headers` and to `apps/web/headers.mjs` (the one definition of the web origin's headers), and it
survives the `assets.config._headers` path:

```
$ curl -sD - -o /dev/null https://inbornapp.com/     -> strict-transport-security: max-age=31536000
$ curl -sD - -o /dev/null https://app.inbornapp.com/ -> strict-transport-security: max-age=31536000
```

No `includeSubDomains` and no `preload`. Each origin speaks for itself: `includeSubDomains` would bind every future
host on the zone to HTTPS for a year, including one that does not exist yet, and `preload` is the entry that cannot
be withdrawn on our schedule. Zone-wide HSTS is an SSL/TLS setting and needs Zone → Settings → Edit, which this
token deliberately does not have.

**2. `/download` was a 404 that three live buy buttons pointed at.** `STORE_LINKS` in
`apps/mobile/src/web/links.ts` sends the web paywall's App Store, Google Play and Desktop buttons to
`https://inbornapp.com/download`, and the page did not exist. `apps/site/src/pages/download.html` now does: the two
store tiles from the same `{{STORE_ROW}}` token the home page uses (so they read "Opens at launch" until
`STORES_LIVE=1`, in one place), a working tile for the browser version, an honest "Not yet" tile for Windows and
macOS rather than a link that goes nowhere, the GitHub line, and refunds pointing at `/support`. It is in
`sitemap.xml` and in the `llms.txt` Product group.

```
$ curl -s -o /dev/null -w '%{http_code}' https://inbornapp.com/download   -> 200
```

Rendered at 390, 768, 1024 and 1440: `docs/qa/deploy-site/download-390.png`, `-768`, `-1024`, `-1440`.

## What was asked for and deliberately not built

- **A `main_module` Worker that re-parses `_headers` and applies the rules itself.** Not needed. The platform already
  applies them from `assets.config._headers`, which is proven by every header table above and by the per-path
  `Cache-Control` rules arriving too. Putting a Worker in front of every asset request to re-implement a feature the
  runtime provides would be a second mechanism doing the first one's job.
- **`/_headers` returning 404 on the app origin.** It returns the SPA shell, not the file, because
  `not_found_handling: single-page-application` answers every unknown path with `index.html`. That is the setting
  that makes `/paywall` and every other deep link work; turning it off to make one path 404 would break them. The
  file itself is not in the manifest, so nothing is disclosed. On the site, where `not_found_handling` is
  `404-page`, `/_headers` and `/_redirects` both return `404`.

## Cross-origin isolation, end to end

`pn web:smoke` against the same build reports `isolated=true` on the first visit (ready 17.3 s, 32.3 tok/s, threads=6)
and 0 model fetches on the offline visit. That is the COOP/COEP pair being honoured by a real browser, not just two
header lines in a `curl` dump, which is what the `_headers` bug would have silently cost.

---

# Deploy 2 — after rounds 47–52 — 24.9.2026

Re-ran the same command against `main` at `5f15cdc` (fix-design round 52: touch targets, contrast, site horizontal
scroll, empty state):

```
node scripts/deploy-cloudflare.mjs --site --app
```

Both workers rebuilt and uploaded clean: `apps/site/dist` (29 files, 0.7 MB, `check.mjs` passed — 13 pages, no
scripts, no external assets, no dead links, CSP present, no sideways scroll at 390/768) to `inborn-site`, and
`apps/web/dist` (42 files, 28.7 MB) to `inborn-app`. `inborn-www-redirect` was re-uploaded unchanged.

## curl verification

```
$ curl -s -o /dev/null -w '%{http_code}' https://inbornapp.com/                 -> 200
$ curl -s -o /dev/null -w '%{http_code}' https://inbornapp.com/download         -> 200
$ curl -s -o /dev/null -w '%{http_code}' https://inbornapp.com/privacy          -> 200
$ curl -s -o /dev/null -w '%{http_code}' https://inbornapp.com/accessibility    -> 200
$ curl -s -o /dev/null -w '%{http_code}' https://inbornapp.com/sitemap.xml      -> 200
$ curl -s -o /dev/null -w '%{http_code}' https://inbornapp.com/llms.txt         -> 200
$ curl -s -o /dev/null -w '%{http_code}' https://inbornapp.com/_headers         -> 404
$ curl -s -o /dev/null -w '%{http_code}' https://www.inbornapp.com/             -> 301, location: https://inbornapp.com/
$ curl -s -o /dev/null -w '%{http_code}' https://app.inbornapp.com/             -> 200
```

`app.inbornapp.com/` response headers (full dump):

```
HTTP/2 200
strict-transport-security: max-age=31536000
content-security-policy: default-src 'self'; script-src 'self' 'wasm-unsafe-eval' blob:; worker-src 'self' blob:; style-src 'self' 'unsafe-inline'; img-src 'self' data: blob:; font-src 'self' data:; connect-src 'self' https://models.inbornapp.com; manifest-src 'self'; object-src 'none'; base-uri 'self'; frame-ancestors 'none'; form-action 'none'
cross-origin-embedder-policy: require-corp
cross-origin-opener-policy: same-origin
cross-origin-resource-policy: same-origin
permissions-policy: camera=(), microphone=(), geolocation=(), payment=(), usb=(), interest-cohort=()
referrer-policy: no-referrer
x-content-type-options: nosniff
x-frame-options: DENY
```

The COOP/COEP pair, HSTS, and the full CSP all arrive as before — the `_headers`-as-config path from the first
deploy still holds on a fresh upload.

## No horizontal scroll at 390

`node apps/site/check.mjs` passed (see build log above: "no sideways scroll at 390/768"). Fetched the deployed
`https://inbornapp.com/site.css` and diffed it byte-for-byte against the local `apps/site/dist/site.css` built by
this run — identical. The fix-design rule that removed the overflow is present at the same line as in source
(`apps/site/src/site.css:183`):

```css
/* A grid track is min-content wide by default, so the 760px models table pushed the whole page sideways below 1024px. */
.band { border-top: 1px solid var(--rule); padding: clamp(40px, 5vw, 64px) 0; display: grid; grid-template-columns: minmax(0, 1fr); gap: 20px; }
```

`grid-template-columns: minmax(0, 1fr)` (rather than a bare `1fr`) is the fix: it caps the track's minimum size
instead of letting it grow to the width of its widest child (the 760px models table), which is what was pushing the
page sideways below 1024px.

## Result

Both origins are live and correct after this deploy. Nothing in the working tree was committed by this pass; `main`
was not touched.

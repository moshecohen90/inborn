# iOS TestFlight build 1.0.0 (4) — archived 11.9.2026, uploaded 13.9.2026

Same recipe as build 3 (`docs/qa/ios-build-3-2026-09-11.md`: portal prerequisites, archive/export/upload commands, device procedure).
Reason for the build: fixes-r9 (F1–F15, incl. **F11 relative document paths** — a data-loss fix that had to ship) merged to `main` after build 3.
`ios.buildNumber` 3 → 4. Share extension included, Instant bundled, Tesseract OCR inputs vendored.

New native bits since build 3, both **Android-only, no-ops on iOS**: `apps/mobile/modules/read-aloud` (`expo-module.config.json`
`platforms: ["android"]`, no podspec, so autolinking skips it on iOS) and `plugins/withStoredNightMode.js` (`withMainActivity` + a
`withDangerousMod(config, ["android", …])`; nothing touches the Xcode project, Info.plist or entitlements).

## Build
Archive from `ios-build-3` = main **c5fff48** (fixes-r9 complete, F15, README) + the buildNumber commit: 11.9 20:21:39 → 20:24:45,
`** ARCHIVE SUCCEEDED **`, no errors, provisioning regenerated by `-allowProvisioningUpdates`. Archive `.app`: `instant.gguf` 532,517,120 B,
`PlugIns/AskInborn.appex` (CFBundleVersion 4), `DocExtractOcr.bundle/tessdata`, ExpoBattery in the binary, App Group in both signed
entitlements. Export 13.9 21:58: `** EXPORT SUCCEEDED **`, `Inborn.ipa` **560,906,339 bytes** (535 MB), app and extension on their
"iOS Team Store Provisioning Profile". main moved on after the archive (590323e: docs, the F16 launch-log line in chat, soak/purchases docs);
none of it is native, and the lead's call was to ship the intact archive.

Upload 13.9.2026: `altool --validate-app` → `VERIFY SUCCEEDED with no errors` (22:31), `--upload-app` → `UPLOAD SUCCEEDED with no errors`
(22:35). ASC build **1.0.0 (4)** id `b1d3f96d-b171-4e56-870a-c7046b12281a`, uploaded 22:36 (Israel), `processingState` **VALID**, not expired,
listed by `GET /v1/betaGroups/25d74408-8d6f-40fb-a7c7-dff71130036e/builds` ("Inborn internal": 4 VALID, 3 VALID, 2 VALID, 1 expired).

## Device check (iPhone 13 Pro, iOS 26.6.1, archived .app via devicectl, no UI automation)
| check | result | evidence |
|---|---|---|
| install the archive's `.app` | `devicectl device install app` → "App installed" | — |
| launch → first screen | onboarding welcome "Nothing leaves this phone.", chip `RUNS ON: A15 BIONIC · 6 GB`, Continue enabled; process alive at 40 s (pid 4784) | `build3/device4/01-first-launch.png`, `02-after-40s.png` |
| bundled Instant adopted | `vault.json`: `via: "bundled"`, sha256 `bd258782…dc517` = catalog, `verifiedAt` 532 ms after `installedAt` | `build3/device4/vault.json` |
| share sheet, Instant answer, Proof OUT 0 B, Hebrew OCR | need taps → **unverified on device** (same as build 3; covered by the merged rounds' simulator/phone proofs) | — |
| clean exit | `SIGTERM` sent, "App uninstalled.", 0 Inborn apps listed, home screen | `build3/device4/03-home-after.png` |

Screenshots live in the session scratch dir (not committed).

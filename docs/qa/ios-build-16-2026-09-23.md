# iOS TestFlight build 1.0.0 (16) — 23.9.2026

Same recipe as builds 3–15 (`docs/qa/ios-build-3-2026-09-11.md` holds the portal prerequisites and every command;
`ios-build-15-2026-09-23.md` the previous run). Branch `ios-build-16`, base `origin/main` **bab0618**, worktree
`/Users/moshecohen/dev/inborn-wt/ios-build-16`. `ios.buildNumber` 15 → 16, `version` unchanged at 1.0.0.

**Why build 16 exists.** Build 15 was cut before rounds 34–43 merged, so nothing those rounds fixed has ever run on
Apple hardware. `docs/qa/acceptance/ios/README.md` says so in as many words: *"the round-43 fixes have not been seen
on the iPhone."* This build carries them to TestFlight and to the phone.

**The archive is stamped with this branch's own commit.** The build-number bump was committed **before** the archive
ran, so `EXConstants.bundle/app.config` carries `extra.commit` **e8a880472467**, this branch's tip, whose parent is
`origin/main` bab0618. The phone's About screen shows the same string (`docs/qa/ios-device-pass-16/r-02-about.png`).

## Build

```
cd apps/mobile && npx expo prebuild -p ios --no-install && (cd ios && pod install)
ruby scripts/ios-add-storekit-tests.rb ios/Inborn.xcodeproj Inborn
cd ios && xcodebuild -workspace Inborn.xcworkspace -scheme Inborn -configuration Release \
  -destination "generic/platform=iOS" -archivePath build/Inborn.xcarchive -derivedDataPath build/dd \
  -allowProvisioningUpdates DEVELOPMENT_TEAM=NGCHN95667 archive
eval "$(../../../scripts/asc-key-env.sh)"
xcodebuild -exportArchive -archivePath build/Inborn.xcarchive -exportOptionsPlist ExportOptions.plist \
  -exportPath build/export -allowProvisioningUpdates -authenticationKeyPath "$INBORN_ASC_KEY_PATH" \
  -authenticationKeyID "$INBORN_ASC_KEY_ID" -authenticationKeyIssuerID "$INBORN_ASC_ISSUER_ID"
xcrun altool --upload-app -f build/export/Inborn.ipa -t ios --apiKey "$INBORN_ASC_KEY_ID" --apiIssuer "$INBORN_ASC_ISSUER_ID"
```

**`--cleanup` is deliberately not in that list** (F91: `asc-key-env.sh --cleanup` deletes every stream's staged key,
not the caller's own). The two things a fresh worktree needs, both of which build 15 learned the hard way, were done
before anything was built: the **`.models` symlink existed before `pod install`** (F164 — without it the podspec
silently drops Tesseract and Hebrew scans stop being read), and **`ExportOptions.plist` was recreated by hand**,
because it is untracked and the prebuild rewrites `ios/` without it.

The archive waited **7 minutes** for `android-vc20`'s `gradle.running`, then ran **22:26:52 → 22:30:08 (3 min 16 s)**,
`** ARCHIVE SUCCEEDED **`, **0** `error:` lines. The export and every driver session took the same lock in turn.

| archive check | value |
|---|---|
| `CFBundleVersion` / `CFBundleShortVersionString` | **16** / 1.0.0, team NGCHN95667 |
| `MinimumOSVersion` | 17.0 |
| `PlugIns/AskInborn.appex` | present, `com.inbornapp.mobile.share-extension`, CFBundleVersion **16** |
| `instant.gguf` | 532,517,120 B |
| `DocExtractOcr.bundle/tessdata` | `eng.traineddata`, `heb.traineddata` — **present on the first archive** |
| ExpoBattery in the binary | `strings Inborn \| grep -c ExpoBattery` = 3 |
| App Group in signed entitlements | `group.com.inbornapp.mobile` on **both** app and appex |
| archive commit | `extra.commit` **e8a880472467**, `ios.buildNumber` 16, `version` 1.0.0 |
| `.app` size | 572 MB, `main.jsbundle` **6,288,554 B** (build 15: 6,157,060 B, **+131,494 B** — rounds 34–43) |

## Export, upload, VALID — all clean on the first try

The export ran **22:30:26 → 22:31:03 (37 s)**, `** EXPORT SUCCEEDED **`, first attempt. F90 did not recur.

| step | value |
|---|---|
| IPA | **561,109,972 B**, sha256 `879d3eb1d39caf4d2d65cdcb427212ded5e194842e5e69939d401de5c89e0415` |
| upload | **22:31:05 → 22:32:36**, `UPLOAD SUCCEEDED with no errors`, 64.0 MB/s |
| Delivery UUID | `290d92c2-66f2-4a21-8ab0-f1bece8a331f` |

**VALID at 22:48:19**, sixteen minutes after the upload finished, with no compliance question and nothing to answer:

| App Store Connect | value |
|---|---|
| build id | `290d92c2-66f2-4a21-8ab0-f1bece8a331f` |
| `processingState` | **VALID** |
| `version` / `minOsVersion` | 16 / 17.0 |
| `buildAudienceType` | `APP_STORE_ELIGIBLE` |
| `usesNonExemptEncryption` | false |
| `uploadedDate` / `expirationDate` | 2026-09-23T12:39:15-07:00 / 2026-12-22 |
| beta group | `16:VALID` alongside 15 and every build back to 1 |

Read from the ASC API, not the web UI: `scratchpad/ios-build-16/poll16.mjs` → `poll.log`, `asc-build16.json`.

## Tests

`pnpm typecheck`, `pnpm lint`, `pnpm test` and `check:store` all green on the branch before the archive:
**1,220 tests** (core 655, mobile 541, i18n 11, ui 13), 0 failed. The counts are above the ones COMMON quotes because
rounds 34–43 landed on `main` between that brief and this build.

The device pass on this archive: `docs/qa/ios-device-pass-16-2026-09-23.md`.

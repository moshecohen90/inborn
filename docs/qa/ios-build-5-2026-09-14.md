# iOS TestFlight build 1.0.0 (5) — 14.9.2026

Same recipe as builds 3/4 (`docs/qa/ios-build-3-2026-09-11.md` has the portal prerequisites and every command; `ios-build-4-2026-09-13.md` the
second run). Reason: fixes-r10 rounds 10–10d on `main` 10cc476 (F17/F18/N1, full-disk handling, screen-reader announcements incl. the O12 iOS
VoiceOver row fix, Android hardware-keys module, httpsDelivery rename/resume fixes). `ios.buildNumber` 4 → 5. Share extension included,
Instant bundled, Tesseract OCR inputs vendored. `ios/` was deleted before the prebuild (clean native tree).

New native bit since build 4: `apps/mobile/modules/hardware-keys` — `platforms: ["android"]`, no podspec, autolinking skips it on iOS.
`app.config.ts` on main differs from build 4 only by the merged buildNumber line; no plugin or iOS config change in rounds 10–10d.

## Build
Archive from `ios-build-3` **f78ffdd** = main 10cc476 + the buildNumber-5 commit: 14.9 08:12:06 → 08:15:12 (3 min 6 s), `** ARCHIVE
SUCCEEDED **`, no errors. Archive `.app`: CFBundleVersion 5 on the app and on `PlugIns/AskInborn.appex`, `instant.gguf` 532,517,120 B,
`DocExtractOcr.bundle/tessdata`, ExpoBattery in the binary, `group.com.inbornapp.mobile` in both signed entitlements. Export 08:15:53:
`** EXPORT SUCCEEDED **`, `Inborn.ipa` **560,916,471 bytes** (535 MB).

Upload 14.9: `altool --validate-app` → `VERIFY SUCCEEDED with no errors` (08:31), `--upload-app` → `UPLOAD SUCCEEDED with no errors` (08:35).
ASC build **1.0.0 (5)** id `4485e4b9-0382-432f-8731-1349efa0a6a7`, `processingState` **VALID**, not expired, uploaded 08:42 (Israel);
`GET /v1/betaGroups/25d74408-8d6f-40fb-a7c7-dff71130036e/builds` ("Inborn internal") lists 5 VALID, 4 VALID, 3 VALID, 2 VALID, 1 expired.

## Device check (iPhone 13 Pro, iOS 26.6.1, archived .app via devicectl, no UI automation)
| check | result | evidence |
|---|---|---|
| install the archive's `.app` | `devicectl device install app` → "App installed" | — |
| launch → first screen | onboarding welcome "Nothing leaves this phone.", chip `RUNS ON: A15 BIONIC · 6 GB`, Continue enabled; process alive at 40 s (pid 5034) | `build3/device5/01-first-launch.png`, `02-after-40s.png` |
| bundled Instant adopted | `vault.json`: `via: "bundled"`, sha256 `bd258782…dc517` = catalog, `verifiedAt` 488 ms after `installedAt` | `build3/device5/vault.json` |
| share sheet, Instant answer, Proof OUT 0 B, Hebrew OCR | need taps → **unverified on device** (as builds 3/4; covered by the rounds' simulator/phone proofs) | — |
| clean exit | `SIGTERM` sent, "App uninstalled.", 0 Inborn apps listed, home screen | `build3/device5/03-home-after.png` |

Screenshots live in the session scratch dir (not committed).
